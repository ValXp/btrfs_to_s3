# AWS credentials for test runs
AWS_ACCESS_KEY_ID=CHANGE_ME
AWS_SECRET_ACCESS_KEY=CHANGE_ME
AWS_SESSION_TOKEN=
AWS_DEFAULT_REGION=us-east-1

# Optional: use a profile instead of access keys
# AWS_PROFILE=default

# Optional: override CLI command (JSON array)
# BTRFS_TO_S3_CMD=["python","-m","btrfs_to_s3"]
